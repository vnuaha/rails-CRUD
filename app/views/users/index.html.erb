<% title 'All users' %>

<%= render 'layouts/header' %>

<h1><%= yield(:title) %></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
